# KeyStone Macro Desk

Macroeconomic dashboard for analysts. 


```
docker compose up -d          # mysql on 3306
cd backend && npm run dev     # api on 4000
cd frontend && npm run dev    # ui on 3000
```

http://localhost:3000

